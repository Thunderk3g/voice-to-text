# Core
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.9.2
pydantic-settings==2.6.1
python-multipart==0.0.17
httpx==0.27.2
tenacity==9.0.0
orjson==3.10.11
python-dotenv==1.0.1

# Workers / queue
celery[redis]==5.4.0
redis==5.2.0
flower==2.0.1

# DB / pgvector
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg[binary]==3.2.3
pgvector==0.3.6
alembic==1.14.0

# Object storage
minio==7.2.10

# Embeddings / ML
sentence-transformers==3.3.1
transformers==4.46.3
torch==2.8.0                             # >=2.8 required by pyannote.audio 4.x
numpy==2.2.6                             # >=2.0 required by pyannote-core 6 (pyannote.audio 4.x); <2.3 keeps numba/umap happy
scikit-learn==1.6.1                      # >=1.6.1 required by pyannote-metrics 4 (pyannote.audio 4.x)
hdbscan==0.8.39
umap-learn==0.5.7

# LLM client (OpenAI-compatible /v1 endpoint; works with Groq, OpenAI, Ollama, vLLM, etc.)
openai==1.55.0

# Sarvam.ai STT (handles Hindi + 10 Indic languages natively)
sarvamai>=0.1.10
pydub==0.25.1                            # duration probe, stereo channel split
# faster-whisper STT (open-source, local; CT2 auto-convert for HF checkpoints
# like Oriserve/Whisper-Hindi2Hinglish-Apex)
faster-whisper==1.0.3
# Local diarization (LOCAL_DIARIZATION=true): pyannote community-1, gated on HF
pyannote.audio>=4.0,<5
# IndicConformer provider (STT_PROVIDER=indic_conformer)
torchaudio==2.8.0                        # must match torch
torchcodec==0.7.0                        # pyannote dep; each minor matches one torch minor (0.7 <-> torch 2.8) but pip can't see that

# Observability
structlog==24.4.0
prometheus-client==0.21.0
# 1.34+ required by pyannote.audio 4.x; instrumentation 0.55b1 is the matching release
opentelemetry-api==1.34.1
opentelemetry-sdk==1.34.1
opentelemetry-exporter-otlp-proto-http==1.34.1
opentelemetry-instrumentation-fastapi==0.55b1
opentelemetry-instrumentation-celery==0.55b1
opentelemetry-instrumentation-sqlalchemy==0.55b1

# Language detection / normalization
# (fasttext-wheel removed: never imported, and its NumPy-1-ABI wheels crash under numpy 2)
langid==1.1.6
indic-transliteration==2.3.67

# Testing
fakeredis==2.36.1
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
pytest-mock==3.14.0
respx==0.21.1
