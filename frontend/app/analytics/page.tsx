"use client";

// /analytics is presently a synonym for the dashboard overview;
// the Sidebar exposes both for navigation discoverability.
import OverviewPage from "../page";

export default function AnalyticsPage(): JSX.Element {
  return <OverviewPage />;
}
